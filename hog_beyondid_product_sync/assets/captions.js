// CAPTIONS — edit only this file to update screenshot captions.
// Key = the screenshot filename (must match the img's src exactly).
// Value = the caption text to show under that image.
// Leave a value as "" to show no caption for that image.

const MANUAL_CAPTIONS = {
    "01_product_sync_overview.png": "The left panel uses Sync Status. Issue Reason remains available as a column and targeted filters.",
    "02_initial_load_list.png": "Initial Product Load creates an auditable batch-based upload process.",
    "03_sync_history.png": "Use Sync History to review execution type, status, duration, API calls and issue counts.",
    "04_reset_full_resync.png": "The reset wizard separates eligible products from excluded products before marking records pending.",
    "05_settings_beyondid.png": "The Settings page stores the API credentials, workspace token and product sync automation options."
};
